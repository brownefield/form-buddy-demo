import AuthorizedAgentForm from './AuthorizedAgentForm'

export const metadata = {
  title: 'Agent Authorization Form — FormBuddy',
  description: 'Authorize an agent to assist you with services and accounts.',
}

export default function AgentIntakePage() {
  return <AuthorizedAgentForm />
}
